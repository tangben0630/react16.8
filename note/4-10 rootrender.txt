

1, 进入 renderroot()

2, 进入循环 调用 workloop 对每棵树每个节点进行遍历 并且更新 
    每个fiber节点上面如果更新的话, 会记录有 updatequeue , 通过update上面是否有内容,

3, workloop过程中, 每个节点更新完成后, 会返回自己的第一个child, 每个都会判断时间片是否
    还有剩余, 没有的话跳出, 有的话继续

4, 一切正常的话 最后把 fiber树变成dom树, 更新页面










4-11 后续补充:

  currentTime: 在一次渲染中产生的更新需要使用相同的时间
                一次批处理的更新中应该得到相同的时间
                挂起任务用于记录的时候应该相同

  expirationTime:
                  

