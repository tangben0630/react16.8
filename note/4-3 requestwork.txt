
1, 加入到 root 调度队列

2, 判断是否批量更新

3, 根据expriationTime判断调度类型



第一步 调用了 addRootToSchedule

第二步 看看要不要 批量更新

第三步 看看是同步还是异步 ExpirationTime的类型是什么  异步调用scheduleCallbackWithExpirationTime
        同步调用 performSyncWork