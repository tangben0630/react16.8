

传说中的 diff:

  为了降低diff复杂度:

    1, 只对同级元素进行diff. 如果一个DOM节点在前后两次中跨越了层级
        react不会复用他
    2, 两个不同类型的元素会产生不同的树 , 如果元素由div --> p 
        react会销毁div及其子节点, 并新建p及其子节点
    3, key




    单一节点的diff:

      最后肯定会生成一个 workInProgress 

      1, 判断上次更新时的 fiber节点(currentFiber)是否存在dom节点
          mount时 不存在dom节点
      2, 



    